# node-terminal drawing

## Instructions

Initialize Node.js REPL

`node`

Load terminaldrawing.js

`.load terminaldrawing.js`

Useful commands

`clear();` to clear terminal

`sleep(500);` to sleep;

Uses color package!
